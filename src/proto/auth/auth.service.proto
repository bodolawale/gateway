syntax = "proto3";

package auth;

message LoginPayload {
  string email = 1;
  string password = 2;
}

message LoginResponse {
  int32 id = 1;
  string email = 2;
  string password = 3;
}

service AuthService {
  rpc Login(LoginPayload) returns (LoginResponse);
}